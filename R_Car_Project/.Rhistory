sink()
sink("lis4369_p2_output.txt")
setwd("C:/Users/shaun/OneDrive/Documents/repos/lis4369/p2")
setwd("C:/Users/shaun/OneDrive/Documents/repos/lis4369/p2")
sink("lis4369_p2_output.txt")
#Read URL containing mtcars dataset
url = "https://vincentarelbundock.github.io/Rdatasets/csv/datasets/mtcars.csv"
mtcars <- read.csv(file=url, head=TRUE,sep=",")
#1.Display contents of the entire dataset
print(mtcars)
#2.Display the first 10 records:
head(mtcars)
#3.Display the last 10 records:
tail(mtcars)
#4.Display the file structure:
str(mtcars)
#5.Display column names:
colnames(mtcars)
#6.Display 1st record/row with column names:
head(mtcars, 1)
#7.Display 2nd column data (mpg), using column number:
mtcars[,1]
#8.Display column ata (cyl), using column name:
mtcars$cyl
#9.Display row/column data (3,4), that is, one field, using square bracket notation:
mtcars[3,4]
#10. Display all data for cars having greater than 4 cylinders:
mtcars[mtcars$cyl > 4,]
#11. Display all cars having more than 4 cylinders 8and* greater than 5 gears:
mtcars[mtcars$cyl > 4 & mtcars$gear > 5,]
#NOTE: There are no rows with those parameters, i will print rows with more than 4 cy1 with 5 gears
mtcars[mtcars$cyl > 4 & mtcars$gear == 5,]
#12. Display all cars having more than 4 cylinders and exactly 4 gears:
mtcars[mtcars$cyl > 4 & mtcars$gear == 4,]
#13. Display all cars having more than 4 cylinders or exactly 4 gears:
mtcars[mtcars$cyl > 4 | mtcars$gear == 4,]
mtcars[mtcars$cyl > 4 & mtcars$gear != 4,]
#15. Display total number of rows (only the number):
nrow
ncol
dim(mtcars)
str(mtcars)
meanhp <- mean(mtcars$hp)
medianhp <- median(mtcars$hp)
minhp <- min(mtcars$hp)
maxhp <- max(mtcars$hp)
quantilehp <- quantile(mtcars$hp)
variancehp  <- var(mtcars$hp)
stdhp <- sd(mtcars$hp)
cat("a. Mean: [1] ", meanhp)
cat("b. Median: [1] ", medianhp)
cat("c. Min: [1] ", minhp)
cat("d. Max: [1] ", maxhp)
cat("e. Quantile: ", quantilehp)
cat("f. Variance: [1] ", variancehp)
cat("g. Standard Deviation: [1] ", stdhp)
summary(mtcars$hp)
library(ggplot2)
qplot(wt, mpg, data=mtcars, main = "Shaun Timothee: mpg vs wt")
plot(mtcars$wt, mtcars$mpg, main = "Shaun Timothee: mpg vs wt")
sink()
library(ggplot2)
qplot(wt, mpg, data=mtcars, main = "Shaun Timothee: mpg vs wt")
plot(mtcars$wt, mtcars$mpg, main = "Shaun Timothee: mpg vs wt")
sink()
